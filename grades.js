let prompt = require('prompt-sync')();
// Require the 'prompt-sync' to take what the user has fed in
let input = prompt('Enter student\'s marks:');
//The user is asked to feed in the student's marks
let fedinMarks = parseFloat(input);
//parse the user input into a value and store it in the variable 'fedinMarks'

//The function 'grades' extracts 'marks' as a parameter and returns a grade

function grades(marks) {
if (marks > 79 && marks < 100) {
   return "A"
    //if marks range from 80-99 return 'A'
} else if (marks >= 60 && marks <= 79){
   return "B"
    //if marks range from 60-79  return 'B'
} else if (marks >= 50 && marks <= 59){
   return "C" 
   // if marks range from 50-59 return 'c'
}else if (marks >= 40 && marks <= 49){
   return "D"
    // if marks range from 40-49 return 'E'
} else {
   return "not graded"
    // if the grade doesnt match any of the ones stated above, return 'not graded'
  }

}

   // call 'grades' function with 'fedinMarks' as an argument and display the outcome
   console.log("student's grade:" + grades(fedinMarks));
































































































































































































































































































































































`  `
       




    










    





